// import { Router } from 'express';
// import passport from 'passport';
// //import { mensajesController } from '../controllers/messages.controller';
// import { tokenOrApiKeyIsValid } from '../middlewares/auth.middleware';
// const router = Router();

// router.get('/:email', tokenOrApiKeyIsValid, mensajesController.getByEmail);

// export default router;
